Refer to the BlockingQueue example we discussed in class.
Explain why the methods enqueue and dequeue need to be synchronized?

To protect access to shared data, the queue implemented using a LinkedList.
When one thread is enqueuing an element, another thread should not be dequeuing it at the same time.